services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: lowtrans
      POSTGRES_PASSWORD: lowtrans
      POSTGRES_DB: lowtrans
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lowtrans"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:

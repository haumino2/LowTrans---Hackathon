# Production: set to your Render API URL (no trailing slash)
# Example: https://lowtrans-api.onrender.com
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional shared API key if API_KEY is set on the backend
# NEXT_PUBLIC_API_KEY=
